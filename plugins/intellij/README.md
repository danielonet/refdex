# RefDex IntelliJ plugin

Placeholder for Phase 6. The Kotlin plugin (IntelliJ Platform Gradle plugin) will launch the bundled
`refdex` daemon executable, write MCP config for installed AI plugins, and provide a status widget,
a "Reindex project" action and a settings page. It does not use PSI; all indexing lives in the daemon.
